

# Change "نماذج" to "ملفات" in Sidebar

Simple text change in the sidebar navigation item for the Form Templates page.

## Change

**`src/components/layout/Sidebar.tsx`** — In the "أخرى" group, change the `name` of the `/form-templates` item from `"نماذج"` to `"ملفات"`.

